class Color {
    public colorId: number;
    public colorName: string;

    public constructor(colorId: number, colorName: string) {
        this.colorId = colorId;
        this.colorName = colorName;
    }
}

export default Color;
